[phases.setup]
providers = ["python", "node"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "npm ci",
    "npm ci --prefix wizard"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
