/**
 * UI color constants for buttons and interactive elements
 */

export const RANDOM_BUTTON_COLORS = {
  background: '#1899dd',
  hover: '#0088cc',
} as const;

/**
 * Layout height constants
 * Adjust STEP_PANEL_HEIGHT to control the step container height on all screen sizes
 */
export const LAYOUT_HEIGHTS = {
  STEP_PANEL_HEIGHT: '41.6rem', // Adjust this value to change step panel height
} as const;
